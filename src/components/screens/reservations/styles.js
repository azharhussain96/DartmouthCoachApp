import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  name: {
    color: 'black',
    fontSize: 20,
    fontWeight: 'bold',
    marginLeft: 10,
    marginTop: 20,
  },
  trip: {
    color: 'black',
    fontSize: 20,
    marginLeft: 10,
    marginTop: 10,
  },
})

export default styles
